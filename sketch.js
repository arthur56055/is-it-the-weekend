var answer = 'NO'

    
function setup(){
  createCanvas(windowWidth, windowHeight)
}

function draw(){
    background('dodgerblue')
    textSize(32)
    fill('white')
    text('Is it the weekend ' + answer, 40, 40)
}
